# Nombre de la aplicaci�n
spring.application.name=mvcDemo

# --- Configuraci�n de Thymeleaf ---
# Desactiva el cach� durante el desarrollo para ver los cambios en las plantillas HTML al instante.
# Para producci�n, es recomendable cambiarlo a 'true' para un mejor rendimiento.
spring.thymeleaf.cache=false

# --- Configuraci�n de la Base de Datos para Render (PostgreSQL con JDBC Manual) ---
# Se utilizan variables de entorno (${...}) para no exponer las credenciales en el c�digo.
# Render proveer� los valores para estas variables autom�ticamente cuando vincules la base de datos.
spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_DATABASE_USERNAME}
spring.datasource.password=${JDBC_DATABASE_PASSWORD}

# Driver de PostgreSQL para JDBC
spring.datasource.driver-class-name=org.postgresql.Driver